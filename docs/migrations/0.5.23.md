# 0.5.23: contract fixture tanısı

Tür: tooling hata mesajı iyileştirmesi. Uygulama kaynağı migration'ı yoktur.

`origin-check-contracts`, manifest'in işaret ettiği fixture veya schema dosyası bulunamadığında ham
Node.js `ENOENT` stack trace'i yerine eksik dosyanın proje-relative yolunu gösterir. Geçersiz JSON
dosyaları da dosya türü ve parse nedeniyle birlikte raporlanır.

Mevcut proje yalnız fixed paket grubunu yükseltmelidir:

```bash
pnpm update "@originloom/*" --latest
pnpm contracts:fixtures
```
