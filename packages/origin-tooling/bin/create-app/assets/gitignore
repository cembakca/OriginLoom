# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
*.tsbuildinfo

# Env — commit .env.development / .env.production, keep local overrides out
.env.local
.env.*.local

# Migration backups — origin-migrate writes the previous copy of every file it
# changes here, including deleted ones. Useful locally, never a repo artefact.
.originloom/backups/

# Test / tooling
coverage/
.vite/
playwright-report/
test-results/
.lighthouseci/
load-test/reports/
artifacts/sbom/

# OS / editor
.DS_Store
*.log
