apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: origin-loom-ingress-boundary
spec:
  podSelector:
    matchLabels:
      app: origin-loom
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: ingress-nginx
      ports:
        - protocol: TCP
          port: 3005
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: monitoring
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: operations
      ports:
        - protocol: TCP
          port: 9090
