apiVersion: v1
kind: Service
metadata:
  name: ssr-kit-operations
  labels:
    app: ssr-kit
spec:
  type: ClusterIP
  selector:
    app: ssr-kit
  ports:
    - name: operations
      port: 9090
      targetPort: metrics
